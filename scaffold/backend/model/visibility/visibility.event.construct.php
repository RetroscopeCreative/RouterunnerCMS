<?php
/**
 * Created by PhpStorm.
 * User: csibi
 * Date: 2014.10.23.
 * Time: 16:23
 */

/*
if (isset($runner->context["reference"], $runner->context["model_class"])) {
	$router = false;
	$model = false;
	$context = array(
		"self" => array("reference" => $runner->context["reference"])
	);
	\runner::return_route("/model/" . $runner->context["model_class"], $context, $router, $model);

	$runner->backend_context = $router->runner->backend_context;

	$runner->context["model"] = $model;
}
*/